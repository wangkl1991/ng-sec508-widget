import { Button508Directive } from './button508.directive';

describe('Button508Directive', () => {
  it('should create an instance', () => {
    const directive = new Button508Directive();
    expect(directive).toBeTruthy();
  });
});
